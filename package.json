{
  "name": "jquery-auto-builder",
  "description": "An Automated jQuery Custom Builder that generates a custom build of jquery based on the needs of your code.",
  "main": "bin/jquery-auto-builder.js",
  "version": "0.1.3-c",
  "engines": {
    "node": ">= 0.10.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/vicnicius/jquery-auto-builder.git"
  },
  "preferGlobal": "true",
  "bin": {
    "jquery-auto-builder": "bin/jquery-auto-builder.js"
  },
  "dependencies": {
    "async": "^0.2.10",
    "minimist": "~0.1.0",
    "requirejs": "^2.1.11",
		"mkpath": "^0.1.0"
  },
  "devDependencies": {
    "grunt-contrib-jshint": "~0.7.2",
    "grunt-contrib-watch": "~0.5.3",
    "load-grunt-tasks": "^0.4.0"
  }
}
